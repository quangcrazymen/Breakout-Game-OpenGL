#pragma once
#include "video/EBuffer.h"

namespace DalatEngine {
	namespace Video {
		class IVideoDriver {
			struct SbufferDesc {
				//explicit SbufferDesc(E_BUFFER_TYPE = EBT_VERTEX,)
			};
		};
	}
}
